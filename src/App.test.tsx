// 簡単な動作確認用のコンポーネント
// もしApp.tsxでエラーが出る場合は、このコンポーネントに置き換えて確認してください

function SimpleApp() {
  return (
    <div style={{ padding: '2rem' }}>
      <h1>動作確認中</h1>
      <p>このメッセージが表示されていれば、Reactは正常に動作しています。</p>
    </div>
  );
}

export default SimpleApp;

